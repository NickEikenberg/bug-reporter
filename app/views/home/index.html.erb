
<div class="container">
  
  <h1>Bug Reporting App</h1>

<% if user_signed_in? %>

        

  <h3><%= link_to 'Create a new project', new_project_path, class:"nav-link"  %></h3>

  <table class="table table-striped table-bordered table-hover">
  <thead>
    <tr>
      <th>Projects</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= project.name %></td>
        <td><%= link_to 'Show', project %></td>
        <td><%= link_to 'Edit', edit_project_path(project) %></td>
        <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<% else %>
  <h3>Please sign up to create or join a project</h3>
<% end %>
</div>


